node_modules/
.env
.env.local
.env.*.local
.vercel/
.mimosa/
*.log
.DS_Store

# Next.js
.next/
out/
next-env.d.ts

# Supabase CLI local state
supabase/.temp/

# Local test database files
tests/db-harness/pgdata/

# Legacy marketing OS — separate system, kept on disk outside this repo
# (per project rule: the two dashboards/systems are never mixed)
legacy-marketing-os/

# TypeScript build cache
tsconfig.tsbuildinfo
*.tsbuildinfo
