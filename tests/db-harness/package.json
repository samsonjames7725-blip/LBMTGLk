{
  "name": "lbmt-db-harness",
  "private": true,
  "type": "module",
  "description": "Local migration verification harness: throws up an embedded PostgreSQL, stubs the Supabase environment (auth/storage schemas, roles), applies supabase/migrations + seed, and runs structural + RLS checks. Never connects to the hosted project.",
  "scripts": {
    "test": "node harness.mjs"
  },
  "dependencies": {
    "embedded-postgres": "^17.10.0-beta.17",
    "pg": "^8.13.0"
  }
}
